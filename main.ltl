// ltl-fastq/main.ltl
//
// FASTQ reader with quality-score histograms.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-fastq v0.1.0")
    io::println("FASTQ reader with quality-score histograms.")
    0
}
