// examples/demo.ltl — runnable demo for ltl_fastq

import std::io
use ltl_fastq as lib

fn main() {
    io::println("ltl_fastq demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}